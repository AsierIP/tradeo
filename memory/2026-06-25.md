- IBKR/Tradeo: the user created a new IB paper account with alias "Tradeo" and asked Tradeo to use it. IBKR API visibility showed exactly one managed DU paper account; configured `TRADEO_IBKR_ACCOUNT` explicitly in `.env`. Do not store or repeat the full account ID in memory.
- Added a hard IBKR blocked-account safety gate via `TRADEO_IBKR_BLOCKED_ACCOUNTS`; use it for the user's personal IB account ID once provided/visible, without storing the full ID in memory.
- IBKR account mapping clarified: alias Malurus is the user's personal account and must be blocked; alias Tradeo is the account intended for Tradeo. Do not store full account IDs in memory.
- UI preference: do not show rejected Research patterns or rejected counts/fallbacks in dashboard tables; keep rejected data internal only.
- IBKR/Tradeo: the user created a new IB paper account with alias "Tradeo" and asked Tradeo to use it. IBKR API visibility showed exactly one managed DU paper account; configured `TRADEO_IBKR_ACCOUNT` explicitly in `.env`. Do not store or repeat the full account ID in memory.
- Added a hard IBKR blocked-account safety gate via `TRADEO_IBKR_BLOCKED_ACCOUNTS`; use it for the user's personal IB account ID once provided/visible, without storing the full ID in memory.
- IBKR account mapping clarified: alias Malurus is the user's personal account and must be blocked; alias Tradeo is the account intended for Tradeo. Do not store full account IDs in memory.
- UI preference: do not show rejected Research patterns or rejected counts/fallbacks in dashboard tables; keep rejected data internal only.
- Research scheduling preference: keep both Research daily and intraday pattern searches running continuously. Daily discovery scheduler should remain enabled; intraday research should remain enabled with 15m and 5m timeframes.
- IBKR data issue observed: historical bars were failing with Error 162 about Trading TWS session connected from a different IP address. If Research runs show sampled windows as 0, check for another TWS/IB Gateway/Client Portal session using the market data connection.
- Frontend/dashboard behavior: after hiding rejected patterns, intraday runs still need to remain visible. A backend fix was applied so `/research/runs?cadence=intraday` filters cadence/timeframe in SQL before returning runs, avoiding slow/empty-looking intraday tables.
- User asked that when the Daily and intraday Research parallel/continuous-search check is done, the result should be sent as an audio message in this Telegram chat.
- Research intraday workflow check: intraday was running continuously on 15m/5m and rejecting weak clusters correctly, but the green Research endpoint omitted `LAB_WATCHLIST`, hiding four non-rejected intraday patterns from the UI. Added `LAB_WATCHLIST` to `_GREEN_RESEARCH_STATUSES`, verified tests and `/research/discovered-patterns?cadence=intraday&visibility=green` returns the four 15m watchlist patterns.
- Intraday paper execution preference: user wants Tradeo to submit paper orders whenever a valid laboratory entry signal is found. Enabled `TRADEO_INTRADAY_PAPER_ENABLED=true` plus intraday limits in `.env` (`max_open_positions=4`, `max_trades_per_day=8`, `max_trades_per_symbol=2`, pacing budget 20). Live remains disabled; do not enable live without explicit confirmation.
