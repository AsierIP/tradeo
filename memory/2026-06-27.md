# Memory - 2026-06-27

## Tradeo distributed intraday workers work

- User is scaling Tradeo first for Research/Lab only, not Fox/live execution. They asked to make it work end-to-end and later compare pattern-search speed multiplier.
- A clean worktree/branch was created for the distributed workers work: `/home/vboxuser/tradeo-worktrees/intraday-distributed-workers-v1` on branch `feature/intraday-distributed-workers-v1`.
- Initial corrected patch/zip from user was applied/validated there. Important additions included intraday work descriptor/fingerprints, `IntradayDistributedWorkQueue`, `IntradayWorkItem`, `IntradayWorkerHeartbeat`, docs, tests, and Alembic migration `0003_intraday_distributed_work_queue.py`.
- Corrections made on top of user files: fixed `models.py` hunk manually because patch failed by context, removed duplicate SQLAlchemy indexes that broke SQLite/Alembic, removed unused `timedelta` import, added `reap_expired_leases()` inside `claim_batch()`, and added missing Alembic migration for existing DBs.
- Validation before runtime work: `ruff check` OK, targeted queue+migration tests 8 passed, `make test-safety` 316 passed with 1 warning, `git diff --check` OK.
- During deployment/runtime setup, Docker backend/worker initially used old image and DB lacked new tables. Backend/worker were rebuilt, Alembic run, and services became healthy.
- Alembic exposed an existing blocker in migration `0002`: PostgreSQL JSONB expression was incorrectly parenthesized. This was fixed so `alembic upgrade head` could complete.
- Important finding: the queue infrastructure alone was not connected to the scheduler, so by itself it did not speed pattern search. A first real acceleration was then implemented: intraday research now runs configured timeframes as separate scheduler jobs, specifically `intraday_research_15m` and `intraday_research_5m`, without touching Fox/orders.
- `.env`/runtime flag was enabled for this intraday parallelization, backend/worker rebuilt/restarted, and stale orphan `running` discovery runs were closed using the system watchdog with a one-off zero threshold so new jobs could start.
- Runtime confirmation: two intraday discovery runs started in parallel, run `#1751` for `5m` at about 19:24:41 Madrid and run `#1752` for `15m` at about 19:24:48 Madrid, only 7 seconds apart. They were observed running concurrently for several minutes, confirming no serial wait.
- Expected speed impact for the first deployed step: intraday 5m and 15m no longer run serially, so full intraday cycle should drop from roughly sum of both timeframes (~30 min previously) to roughly the max of the two (~15-16 min), about x2 for intraday pattern-search cycle time if durations remain similar.
- Need after compaction: continue monitoring runs `#1751/#1752` until completed, compute actual durations and multiplier vs previous serial baseline, then report concise comparison to user in Spanish. Also check `git status`, tests/health after latest runtime edits, and do not commit/push unless user asks.

## Tradeo 10-worker intraday scaling continuation

- User explicitly asked not to commit yet; they want stress testing and results vs previous version, then asked to keep adding workers and improvements to reach 10.
- Work is still in branch/worktree `/home/vboxuser/tradeo-worktrees/intraday-distributed-workers-v1` on `feature/intraday-distributed-workers-v1`; do not assume committed/pushed.
- Before 10-worker work, validated runtime comparison: sequential baseline `#1747 5m + #1748 15m` processed 9300 windows in 30.83 min (~5.03 windows/s, 0 accepted, 47 rejected). Parallel 2-worker runs `#1751 5m + #1752 15m` processed 9300 windows in 17.10 min (~9.07 windows/s, 0 accepted, 47 rejected), measured x1.80 speedup and ~13.74 min saved.
- Stress test before commit: 500 synthetic queue tasks, 20 concurrent workers, 500 unique claims, 0 duplicate claims, duplicate enqueue rejected by fingerprint, expired lease recovered; throughput about 705 tasks/s claim+complete; stress rows cleaned. `make test-safety` was fixed to be reproducible even when `.env` has runtime flags enabled, and passed 319 tests with 1 warning.
- Added real intraday split by symbol chunks. First 4-job configuration split 5m and 15m into two disjoint symbol chunks each. It registered/runs as 4 jobs, but real test only improved from 17.10 min to about 15.83 min for same 9300 windows (only x1.08 over 2-worker mode), showing data-provider/cache contention rather than CPU was bottleneck.
- For reaching 10, implemented/started implementing multiple safeguards: file lock around market data cache writes, shared research context cache in worker so SPY/QQQ/regime are reused per period/timeframe, configurable chunk count for 10 jobs (`5 chunks x 2 timeframes`), longer scheduler interval around 900s for controlled tests, and cleanup of orphan running discovery runs with watchdog before measurements.
- 10-job tests exposed additional bottlenecks: concurrent monthly universe snapshot creation collided on temp files; added locking/caching around universe snapshot. Also IB/data-provider resets occurred because every chunk refreshed data; added global upstream concurrency limit config `TRADEO_MARKET_DATA_UPSTREAM_MAX_CONCURRENCY` and eventually switched intraday research toward cache-first by setting `TRADEO_INTRADAY_RESEARCH_REFRESH_MARKET_DATA_ENABLED=false` and upstream max concurrency to 1.
- Latest stated state before compaction: fourth improvement was ready/deploying: cache-first intraday Research separates market-data refresh from parallel pattern search. Tests around data provider/worker had been run repeatedly (`32 passed`, then `60 passed`, then `64 passed`) and ruff OK after those changes. Need resume by checking current rebuild/deploy status, `git status`, Docker health/logs, whether 10 cache-first chunks completed, and final run timings.
- Important conclusion to preserve: blindly increasing chunks to 10 with live IB refresh was unstable/not a valid speed test. Viable path is 10 calculation jobs operating on warmed/cache-first data plus a separate/paced prefetch/sync stage. User wants improvements necessary to have 10, so continue hardening for stable 10 before commit.
