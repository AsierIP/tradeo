- Created cron `tradeo-new-pattern-alerts` to notify Asier on Telegram about new non-REJECTED patterns. Initially it checked `discovered_patterns` directly, then changed it to consume backend trigger events from `audit_logs.action='new_pattern_discovered'`. State file: `memory/pattern-alert-audit-state.json`, initialized at `last_seen_audit_id=0`.

- Asier asked to be notified only when Tradeo actually creates a new pattern, not by polling old discovered patterns. Implemented/confirmed backend trigger in `NovelPatternRegistry` that writes `audit_logs.action='new_pattern_discovered'` only for newly created non-REJECTED patterns.
- New-pattern Telegram alert should include whether the pattern is daily or intraday, timeframe, long/short side, status/name, quality label (alta/media/baja) and metrics such as score, expectancy, profit factor, reward/risk, samples, and symbols.
- After installing the trigger/alert change, backend and worker were rebuilt/restarted and health checked; registry tests passed (`8 passed`) and ruff passed.
- Recent Tradeo status checked for Asier: backend/worker/DB/Redis healthy; IBKR paper connection healthy with `readonly=false`; intraday research was running and scanning but had accepted 0 new intraday patterns/signals at that time. Scheduler `maximum number of running instances reached` meant long cycle overlap prevention, not IB blocking.
