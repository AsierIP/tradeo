# 2026-06-24

- The user wants Telegram communication with the agent through a bot named Tradeo, as a dedicated channel for Tradeo-specific conversations.
- Configured OpenClaw Telegram account `tradeo` for @TradeoInternalBot with DM pairing. Do not store the bot token in memory.
# 2026-06-24

- The user wants Telegram communication with the agent through a bot named Tradeo, as a dedicated channel for Tradeo-specific conversations.
- Configured OpenClaw Telegram account `tradeo` for @TradeoInternalBot with DM pairing. Do not store the bot token in memory.
- Tradeo order notifications were enabled to this Telegram chat: a watcher runs every minute and alerts on `paper_trade_opened`, `ibkr_bracket_submitted`, and `ibkr_fills_ingested`, with IBKR fills deduplicated by `fill_id_hash`.
- Research system was checked after the user asked to make sure it was working: Tradeo containers were healthy, backend health endpoints responded, worker heartbeat/scheduler were active, IBKR paper responded on port `14002`, and research runs 822/823 completed with 4,500 windows and 24/23 clusters after earlier temporary zero-window runs 818-821.
- Research Daily/Intradía tabs were fixed and deployed: frontend now tracks the active cadence tab, Daily loads `1d` runs/patterns, Intradía loads `5m/15m` runs/patterns, the “Research Lab” action respects the active tab, backend endpoints accept cadence filters for `/research/runs` and `/research/discovered-patterns`, and backend/frontend were rebuilt and restarted healthy.

- Research UI was further adjusted after the user said Daily/Intradía still looked identical: added visible Daily/Intradía comparison cards, cadence labels in rows, and clearer buttons/headers so Daily (`1d`/`5y`) and Intradía (`5m/15m`/`30d`) are visually distinct; frontend/backend were rebuilt and running.
- User preference for Research page: show only “green” patterns, meaning include approved candidates plus `needs_confirmation`, but hide `rejected` and `failed_confirmation`. Implemented with backend `/research/discovered-patterns?visibility=green` and frontend-side safety filtering.
- Research Director behavior was corrected after the user noticed `needs_confirmation` candidates were not being rechecked after a run completed. The run-close review now includes both patterns from the just-finished run and the existing `needs_confirmation` queue, deduplicated by ID. Verified on run 986: `patterns_reviewed = 27`, including 24 rejected learning-memory patterns and 3 `needs_confirmation`; those three were updated at `2026-06-24T16:48:20Z`. Backend and worker were rebuilt/redeployed healthy. Run 987 was marked failed as an orphan from redeploy; run 988 was active afterward.
